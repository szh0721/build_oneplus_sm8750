name: Build_oneplus_13t_sm8750
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    # Set the default working directory for all steps in this job to /mnt
    defaults:
      run:
        working-directory: /mnt
    steps:
      - name: 📊 Check disk space (检查磁盘空间)
        run: |
          echo "构建前磁盘空间："
          df -h

      - name: ⚙️ Set REPO_MANIFEST (设置对应机型配置文件)
        run: |
          echo "REPO_MANIFEST=oneplus_13t" >> $GITHUB_ENV
          echo "DEVICES_NAME=oneplus_13t" >> $GITHUB_ENV

      - name: ⚙️ Set device-specific CCACHE_DIR (设置对应机型Cceche路径)
        run: |
          echo "CCACHE_DIR=$HOME/.ccache_${REPO_MANIFEST}" >> $GITHUB_ENV

      - name: 📦 Configure Git (设置下载账户)
        run: |
          git config --global user.name "Q1udaoyu"
          git config --global user.email "sucisama2888@gmail.com"

      - name: 🛠 Cache APT packages (缓存APT包)
        uses: actions/cache@v3
        with:
          path: /var/lib/apt/lists
          key: ${{ runner.os }}-apt-${{ hashFiles('.github/workflows/Build_oneplus_sm8750.yml') }}
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: 📦 Install dependencies (安装依赖)
        run: |
          echo "更新APT包列表..."
          sudo apt-get update -qq

          sudo apt-get install -y --no-install-recommends \
          python3 git curl ccache libelf-dev \
          build-essential flex bison libssl-dev \
          libncurses-dev liblz4-tool zlib1g-dev \
          libxml2-utils rsync unzip

          ccache --version
          echo "依赖安装完成"

      - name: 🛠 Cache Custom Clang (缓存自定义 Clang 编译器)
        uses: actions/cache@v3
        with:
          path: /mnt/clang
          key: clang-19.0.0git-20240723-${{ runner.os }}
          restore-keys: |
            clang-19.0.0git-20240723-${{ runner.os }}

      - name: 📥 Download and setup Custom Clang (下载并设置自定义 Clang 编译器)
        run: |
         CLANG_DIR="/mnt/clang"

         if [ ! -f "$CLANG_DIR/.clang_setup_complete" ]; then
          echo "下载并解压 Clang..."
          sudo rm -rf "$CLANG_DIR"
          sudo mkdir -p "$CLANG_DIR"
          sudo chown -R $USER:$USER "$CLANG_DIR"
          
          cd "$CLANG_DIR"

          wget -O clang.tar.gz https://github.com/ZyCromerZ/Clang/releases/download/19.0.0git-20240723-release/Clang-19.0.0git-20240723.tar.gz
          tar -xzf clang.tar.gz
          rm clang.tar.gz
          touch .clang_setup_complete

          echo "Clang 下载与解压完成"
         else
          echo "使用缓存的 Clang 编译器"
         fi

         echo "CLANG_PATH=$CLANG_DIR" >> $GITHUB_ENV
         echo "PATH=$CLANG_DIR/bin:$PATH" >> $GITHUB_ENV


      - name: 📥 Restore ccache (载入 机型：${{ env.DEVICES_NAME }}的 ccache 缓存)
        uses: actions/cache@v3
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ runner.os }}-${{ github.ref }}-${{ env.REPO_MANIFEST }}
          restore-keys: |
            ccache-${{ runner.os }}-${{ env.REPO_MANIFEST }}-
            ccache-${{ runner.os }}-

      - name: 📥 Init ccache (如果是第一次运行则初始化Cceche)
        run: |
          export CCACHE_COMPILERCHECK="%compiler% -dumpmachine; %compiler% -dumpversion"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="8G"
          export USE_CCACHE=1

          INIT_FLAG="$CCACHE_DIR/.ccache_initialized"
          if command -v ccache >/dev/null 2>&1; then
            if [ ! -f "$INIT_FLAG" ]; then
              echo "初始化 ccache ($CCACHE_DIR)..."
              mkdir -p "$CCACHE_DIR"
              ccache -M "$CCACHE_MAXSIZE"
              touch "$INIT_FLAG"
            else
              echo "ccache 已初始化，跳过"
            fi
          else
            echo "未安装 ccache，跳过"
          fi

      - name: 📥 Install repo tool (下载repo工具)
        run: |
         curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
         chmod a+x ~/repo
         sudo mv ~/repo /usr/local/bin/repo

      - name: 📥 Initialize repo and sync (初始化repo并同步内核源码)
        run: |
         sudo mkdir -p kernel_workspace
         sudo chown -R $USER:$USER kernel_workspace
         cd kernel_workspace
         repo init -u https://github.com/szh0721/kernel_manifest.git -b refs/heads/oneplus/sm8750 -m ${REPO_MANIFEST}.xml --depth=1
         repo --trace sync -c -j$(nproc --all) --no-tags
         rm kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports!"
         rm kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports!"

      - name: ⚙️ Set up SukiSU Ultra (配置 SukiSU Ultra)
        run: |
         cd kernel_workspace/kernel_platform
         curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/susfs-main/kernel/setup.sh" | bash -s susfs-main
         cd ./KernelSU
         KSU_API_VERSION_LOCAL=$(grep -m1 -oP 'KSU_API_VERSION_LOCAL\s*:=\s*\K\d+' kernel/Makefile || echo "1")
         KSU_COMMIT_HASH=$(git rev-parse --short=8 HEAD)
         KSU_BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)
         KSU_API_VERSION=$(expr $(/usr/bin/git rev-list --count main) "+" 10000 "+" 700)
         KSU_VERSION="v${KSU_API_VERSION_LOCAL}-${KSU_COMMIT_HASH}@${KSU_BRANCH_NAME}\\"
         echo "KSUVER=$KSU_API_VERSION" >> $GITHUB_ENV
         
         escape_sed() {
             echo "$1" | sed 's/[][\/\.*^$]/\\&/g'
         }
         
         OLD_KSU_VERSION=$(grep -m1 -oP 'ccflags-y\s*\+=\s*-DKSU_VERSION=\\"\K[^"]+' kernel/Makefile || echo "\$(KSU_KERNEL_VERSION)\\")
         OLD_KSU_API_VERSION=$(grep -m1 -oP 'ccflags-y\s*\+=\s*-DKSU_API_VERSION=\K\d+' kernel/Makefile || echo "\$(KSU_API_VERSION)")
         
         OLD_KSU_VERSION_ESC=$(escape_sed "$OLD_KSU_VERSION")
         KSU_VERSION_ESC=$(escape_sed "$KSU_VERSION")
         OLD_KSU_API_VERSION_ESC=$(escape_sed "$OLD_KSU_API_VERSION")
         KSU_API_VERSION_ESC=$(escape_sed "$KSU_API_VERSION")
         
         sed -i.bak "s/-DKSU_VERSION=\\\\\"${OLD_KSU_VERSION_ESC}\\\\\"/-DKSU_VERSION=\\\\\"${KSU_VERSION_ESC}\\\\\"/" kernel/Makefile
         sed -i.bak "s/-DKSU_API_VERSION=${OLD_KSU_API_VERSION_ESC}/-DKSU_API_VERSION=${KSU_API_VERSION_ESC}/" kernel/Makefile
        
      - name: 🔧 Set up SUSFS (配置 SUSFS)
        run: |
           cd kernel_workspace
           git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android15-6.6
           git clone https://github.com/SukiSU-Ultra/SukiSU_patch.git

           cd kernel_platform        
           cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android15-6.6.patch ./common/
           cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/
           cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/

           cd ./common
           patch -p1 < 50_add_susfs_in_gki-android15-6.6.patch || true
           cp ../../SukiSU_patch/hooks/syscall_hooks.patch ./
           patch -p1 -F 3 < syscall_hooks.patch
           echo "完成"

      # --- START: ADDED AND CORRECTED STEPS ---
      - name: 🚀 Compile Kernel (编译内核)
        run: |
          cd kernel_workspace/kernel_platform
          
          # Set up build environment for GKI kernels
          source build/envsetup.sh
          lunch vendor/${{ env.REPO_MANIFEST }}_GKI.bp
          
          # Start the build process
          # The LLVM variables ensure our custom Clang is used
          build/build.sh \
            --lto=thin \
            --skip-defconfig \
            --skip-fetch-artifacts

      - name: 📦 Package with AnyKernel3 (使用AnyKernel3打包)
        run: |
          # Clone the AnyKernel3 repository
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git
          
          # Define paths for clarity
          KERNEL_OUT_DIR="/mnt/kernel_workspace/kernel_platform/out/android15-6.6/dist"
          AK3_DIR="/mnt/AnyKernel3"
          
          # Verify that the kernel image and dtbo exist before proceeding
          if [ ! -f "$KERNEL_OUT_DIR/Image" ] || [ ! -f "$KERNEL_OUT_DIR/dtbo.img" ]; then
            echo "Kernel build artifacts not found!"
            exit 1
          fi
          
          # Copy the compiled kernel artifacts into the AnyKernel3 directory
          cp "$KERNEL_OUT_DIR/Image" "$AK3_DIR/"
          cp "$KERNEL_OUT_DIR/dtbo.img" "$AK3_DIR/"
          
          # Navigate into the AnyKernel3 directory to create the zip
          cd "$AK3_DIR"
          
          # Create the final flashable zip file
          zip -r9 "../KSU-SFS-${{ env.DEVICES_NAME }}-$(date +%Y%m%d).zip" ./* -x ".git/*" "README.md" ".github/*"

      - name: ⬆️ Upload Artifact (上传构建产物)
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU-Kernel-${{ env.DEVICES_NAME }}
          path: /mnt/*.zip
      # --- END: ADDED AND CORRECTED STEPS ---
